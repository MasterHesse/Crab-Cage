// src/server.rs
use anyhow::Result;

/// 启动 KV 服务（stub）
pub async fn start() -> Result<()> {
    // 这里先打个桩
    println!("kvdb server is starting... (not yet implemented)");

    // 直接返回 Ok，以便编译通过
    Ok(())
}